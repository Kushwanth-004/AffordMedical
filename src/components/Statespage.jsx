import { Box, Typography } from "@mui/material";
const StatesPage = () => {
  return (
    <Box p={3}>
      <Typography variant="h5">URL Statistics</Typography>
      <Typography mt={2}>Coming soon... (mock clicks, geo, source)</Typography>
    </Box>
  );
};

export default StatesPage;
